package main

func main() {
	/*	var marray = []int{1, 35, 5, 6, 6}

		for i := 0; i < len(marray); i++ {
			fmt.Println(strconv.Itoa(marray[i]) + " is at " + strconv.Itoa(i))
			fmt.Println(strconv.FormatInt(int64(marray[i]), 8) + "  is backend")
		}
		for _, j := range marray {
			plus := mySwitch(j)
			fmt.Println(strconv.Itoa(plus) + " is +1 value")
		}*/

}

/*
func mySwitch(i int) int {
	switch {
	case i > 0:
		return i + 1
	default:
		return 0
	}
}
*/
